# AI Agent 八股（Agent / LLM 工程）

> 简历对应条目：**AI 应用：Agent/LLM 工程（ReAct/Tool Use/上下文工程/HITL/SSE）；RAG/MCP，有生产级 Agent 0→1 产出**。
> 这份笔记按"面试官会怎么深挖这条简历"来组织——重点不是名词解释，而是**每个点背后的取舍和踩过的坑**。

## 0. 面试地图：这条简历会被怎么问

```
① 循环控制     轮次上限、终止条件、死循环、预算   ← 必问，且必追问"为什么是这个数字"
② 范式选型     为什么 ReAct 不用 Plan-and-Execute；为什么/为什么不拆 Multi-Agent
③ 工具         工具怎么定义、参数校验、超时容错、MCP/Skill 区别
④ 上下文       记忆分层、压缩策略、状态外置
⑤ 交互         HITL 什么时候必须介入、SSE 为什么不 WebSocket
⑥ RAG          切分、召回、rerank、检索到了但模型没用
⑦ 评测         怎么衡量输出正确性、怎么衡量"优化有效"
⑧ 工程兜底     并发限流、成本、可观测、幻觉
```

**核心提醒**：写了"生产级 0→1"，**每一个点都要能落到具体数字和真实故障**——token 从多少压到多少、轮次分布是多少、死循环怎么发现的、评测集怎么做。面试官追问路线永远是「**你遇到过什么问题、怎么解决的**」，不是「这个概念是什么」。

---

## 1. Agent 循环控制与预算：「为什么是 6 轮？」

### 轮次分配（从真实交互路径推出来的，不是拍脑袋）

| 轮次 | 用途 | 覆盖的场景 |
|---|---|---|
| **轮 1** | 单步查询（查总数 / 成功率） | **最高频，大部分问题到这就结束** |
| **轮 2-3** | 对比类（两个事件 / 两个阶段的交叉） | 次高频 |
| **轮 4** | 若仍未收敛 → 判定问题有歧义 → **模型主动 clarify**，让用户从候选值里选 | 歧义问题 |
| **轮 5** | 拿到用户补充后继续查 | |
| **轮 6** | **预算最后一轮**：掐断工具调用，要求输出已查到的结论 + **明确列出还缺什么信息** + 引导用户补全 | 兜底 |

### 第 6 轮是关键：把"预算耗尽"从故障变成交互节点

```
不是硬切（返回失败）              → 用户看到半截回答，体验是"故障"
也不是让模型硬编一个结论          → 违反数据可信度
而是：交付部分结论 + 列信息缺口 + 引导用户补全
```

**产品纪律**：不做无数据支撑的推断——缺数据就明确说缺什么。
**行为一致性**：拿不准就问用户（第 4 轮 clarify），预算尽了把球交回用户（第 6 轮收尾）——用的是同一条原则，所以 agent 行为可预测。

### 追问：「为什么是 6，不是 3 也不是 10」→ 三个论证维度

```
① 从数据来（最硬）
   统计线上请求的轮次分布：P50 = 1~2 轮，P90 = 4 轮，P95 = 5 轮
   → 6 是"覆盖 P95 + 一点余量"的位置

② 从成本来
   每轮 = 一次 LLM 调用（延迟 + token）+ 若干工具调用
   6 轮是最坏情况的延迟上限，要能说出单轮 P50 延迟、6 轮能否被用户接受

③ 从边际收益衰减来
   第 7、8 轮还能提升多少正确率？实测提升微小 → 截断点合理
```

**并且要说这个数字是活的**：

> "6 不是拍死的常量，是**可校准的**——我们监控『轮次用尽』的比例。如果 10% 的请求撞到第 6 轮，说明上限太低；只有 0.1% 撞到，说明可以从 6 降到 4 省一半成本。这个数字是拿数据调出来的。"

### 缺口：轮次上限只是**粗粒度**保护，还要有质量层

**真正的失效模式不是"轮次不够"，而是"轮次被浪费"**：

```
· 模型在第 2、3、4 轮反复调用【同一工具、同样参数】→ 结果一样，纯浪费
· 每轮都有一点进展但方向错误（在错误的候选空间里搜索）
· 工具持续报错，模型一直重试同一个调用
```

| 层 | 机制 |
|---|---|
| **数量层** | 轮次上限（6）+ 时间预算 + token 预算 |
| **质量层** | **重复调用检测**（工具+参数指纹去重）、**无进展检测**（连续 N 轮无新增有效信息）、显式死循环识别 |

**质量层要能提前终止**：第 3 轮就检测到"连续两轮同工具同参数"，应**直接跳进第 6 轮那套收尾逻辑**，不傻等。

> 面试话术："轮次上限是兜底，但我不指望它保证质量——真正提前止损的是重复调用检测和无进展检测，命中就直接跳收尾分支。"

### 追问：第 6 轮"禁止再调工具"**怎么强制**

**不能只靠 prompt**（模型不保证遵守）。手段可以叠加：

```
① tool_choice = "none" / 工具列表传空    ← 最硬，从 API 层掐掉
② 系统提示词里明确要求输出结论           ← 软约束
③ 【拦截层】：即使模型返回工具调用也直接拒绝，并返回错误
   → 让它知道"这条路堵死了，该出结论了"
```

**③ 是必须有的 fallback**——只说"通告模型禁止调工具"会被追问"它不听怎么办"。

### 追问：clarify 的轮次语义（**逻辑坑，提前答掉是加分**）

```
问题：第 4 轮 clarify，用户补充后继续查 —— 那算同一个任务还是新任务？预算重置吗？

不重置 → 用户被"模型不会问问题"惩罚了（模型自己需要澄清，却吃掉用户的轮次）
重置   → "6 轮上限"就不成立，用户可以无限喂澄清续命

合理设计：clarify 【单独计数】，不占查询轮次；或部分重置 + 限制 clarify 次数 ≤ 2
```

**连带两个必须能答的点**：

- **"没收敛"怎么判定？** 建议用**规则**（比如"连续两轮没拿到新信息"），别让模型自评（不可靠）。
- **clarify 的候选值从哪来？** 必须是**前几轮查询枚举出来的真实值**（比如查询返回了 3 个 event 名），不能是模型编的——这句话能证明候选值有依据。

### 面试话术（可背）

> "6 轮不是拍脑袋，是三个约束的交点：**一是数据**——线上请求轮次分布 P50 是 1~2 轮、P95 是 5 轮，所以 6 是覆盖 P95 加余量的位置；**二是成本**——每轮一次 LLM 调用加工具调用，6 轮是可接受的延迟上限；**三是边际收益**——实测 7 轮以后正确率提升很小。这个数字还会按『轮次用尽的比例』继续校准。
>
> 轮次分配：第 1 轮覆盖最高频的单步查询，2~3 轮处理对比类问题，连续几轮没有新增有效信息就判定为歧义、主动 clarify 让用户从**前几轮枚举出的真实候选值**里选，最后一轮做收尾——**通过 `tool_choice=none` 掐断工具，要求输出已查到的结论 + 明确列出缺什么信息 + 引导用户补全**。所以预算耗尽不是失败态，是一个自然的交互节点。
>
> 不过轮次上限只是兜底。真正提前止损的是**重复调用检测和无进展检测**——同工具同参数重复调用、连续两轮没新信息，就提前跳收尾，不傻等。
>
> 整个 agent 的原则是一致的：**拿不准就问用户，预算尽了就把球交回用户**，不做无数据支撑的推断。"

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 为什么是 6 不是 3/10 | 数据（轮次分布 P95）+ 成本（延迟）+ 边际收益衰减，且按撞满率持续校准 |
| 怎么检测死循环 | 同工具同参数指纹去重 + 连续 N 轮无新增信息 → 提前进收尾 |
| clarify 后轮次重置吗 | clarify 单独计数不占查询轮次（或部分重置 + 限制 clarify 次数） |
| 怎么保证第 6 轮真的不调工具 | `tool_choice=none` + 拦截层拒绝并返回错误（不能只靠 prompt） |
| 6 轮不够用怎么办 | 收尾交付部分结论 + 列信息缺口 + 引导补全，让用户决定是否继续（不静默失败） |
| 轮次和 token 预算的关系 | 轮次是粗粒度代理；真正的资源约束是延迟和 token，两者都要设上限 |

---

## 2. Agent 范式：ReAct / Plan-and-Execute / Reflexion / Multi-Agent

| 范式 | 机制 | 优点 | 代价 |
|---|---|---|---|
| **ReAct** | Thought → Action → Observation 循环 | 简单、可解释、能根据观察纠错 | 每步一次 LLM 调用 → 延迟和 token 成本高；容易绕圈 |
| **Plan-and-Execute** | 先出完整步骤列表，再逐步执行 | 全局视角、省 token、步骤少 | 计划错了难纠正；环境变化适应性差 |
| **Reflexion** | 执行完自我反思，失败经验写回记忆重试 | 能自我改进 | 多一轮调用 |
| **Multi-Agent** | 按职责分工（Planner / Executor / Critic / Summarizer） | 职责单一、上下文隔离、可并行 | 通信开销、状态一致性、调试难 |

**必答的取舍**：

- **ReAct vs Plan-and-Execute**：延迟成本 vs 适应性。简单单步任务 ReAct 够；步骤多且可预判的任务 Plan-and-Execute 更省。
- **为什么需要 Critic**：**评估者与执行者分离**——让同一个模型自评自判有偏差（它倾向认为自己对了）。
- **为什么拆 Multi-Agent 而不是写成一个更大的 Prompt**：上下文隔离（每个 agent 只看自己需要的）+ 职责单一（prompt 更短更准）+ 可并行。代价是状态一致性和编排复杂度。
- **什么时候不该拆**：任务链路短、状态共享多的时候，拆了反而增加通信和一致性问题——**要能说出"我们为什么不拆"**。

---

## 3. Tool Use / Function Calling

```
机制：把工具签名（name + description + JSON Schema）放进 prompt
     → 模型输出结构化调用 → 应用执行 → 结果回填 → 模型继续
```

**四个工程要点**：

| 点 | 说明 |
|---|---|
| **工具描述质量决定调用准确率** | 这是最有效的优化点，**比换模型管用**——description 写清楚"什么时候用、什么时候不要用、参数含义" |
| **参数校验必须自己做** | 模型会编参数（不存在的 ID、越界值、类型错误）→ 校验失败要返回**可读的错误信息**让模型自我修正 |
| **工具要幂等 + 超时 + 重试 + 熔断** | 一个慢工具能拖垮整个 Agent；重试要防重复副作用 |
| **工具粒度** | 太细 → 轮次爆炸；太粗 → 模型不会用。**一个工具做一件事，但要是"业务动作"粒度** |

---

## 4. MCP / Skill / Function Calling 三者关系（高频混淆点）

```
Function Calling = 模型的【能力】—— 能输出结构化的调用意图
MCP              = 【传输/发现协议】—— 怎么把工具暴露给模型
Skill            = 【打包的能力单元】—— 提示词 + 工具 + 流程的组合
```

**MCP 解决的是 M×N 问题**：M 个模型 × N 个工具，本来要写 M×N 个适配；MCP 标准化成 client-server 协议——工具方实现一个 MCP Server，**任何支持 MCP 的 client 都能用**。

**MCP 的三个原语**：Tools（可执行动作）/ Resources（可读数据）/ Prompts（预置提示模板）。

**为什么用 Skill 而不是把知识写进 Prompt**：

- Prompt 会随对话变长而被稀释、也会占 token；Skill 是**按需加载**的
- Skill 能版本化、能独立演进、能复用
- 但要能说清**什么适合做成 Skill、什么不适合**：稳定的领域知识/固定流程适合；一次性的、强依赖当前上下文的适合写进 prompt

---

## 5. 上下文工程与记忆

### 记忆分层

```
短期记忆   对话历史（滑动窗口）
长期记忆   向量库 / 摘要（跨会话）
工作记忆   当前任务的中间状态
```

### 上下文不是越长越好

```
· 中间部分容易被忽略（lost in the middle）
· 成本随长度线性增长
· 注意力被稀释 → 关键信息被淹没
```

### 压缩手段

| 手段 | 代价 |
|---|---|
| 滑动窗口 | 会丢早期信息 |
| LLM 摘要历史 | 有信息损失、多一次调用 |
| 重要度评分筛选 | 需要设计评分标准 |
| 分层（原始 + 摘要） | 实现复杂 |

### 状态外置（长任务不崩的关键）

**把任务状态写到文件/DB，而不是全塞进上下文**——这样：
- 上下文只保留"当前需要的信息"
- 中断后可恢复（HITL 审批、轮次用完继续都需要）
- 避免"上下文越滚越大直到超限"

### 上下文过长时的信息缺失怎么解

```
分层记忆 + 重要度评分：
  · 把原始信息、摘要、关键实体索引分开存
  · 需要时按问题类型检索回相关片段（而不是全部塞回）
```

---

## 6. HITL（Human-in-the-Loop）

**什么时候必须人工介入**：

```
· 高风险动作：删数据 / 发邮件 / 支付 / 改生产配置
· 低置信度：模型自己表示不确定，或规则判定歧义
· 合规要求：可审计的关键决策
```

**机制**：

```
打断点（checkpoint）→ 持久化状态 → 人工审批 → 恢复执行
```

**本质是"自动化程度 vs 风险"的权衡**：全自动有风险，全人工没价值。设计时要能说清**在哪一步设断点、断点粒度多细**。

**和 clarify 的区别**：clarify 是**模型主动问用户**（信息不足），HITL 是**流程要求人工确认**（风险控制）——两者都让用户介入，但触发原因不同。

---

## 7. SSE 流式输出

**为什么用 SSE 不用 WebSocket**：

| | SSE | WebSocket |
|---|---|---|
| 方向 | **单向**（server → client） | 双向 |
| 协议 | 普通 HTTP（`text/event-stream`） | 需要协议升级 |
| 重连 | **自动重连**，带 `Last-Event-ID` 可续传 | 自己实现 |
| 复杂度 | 简单 | 较高 |

**对话式 AI 只需要单向推流 → SSE 够用且更简单。**

**工程坑（必说）**：

```
① Nginx/网关要关缓冲：proxy_buffering off
   → 否则流式变批式，用户等半天才看到整段
② 要发心跳，防中间层超时断连
③ 前端要处理流式渲染导致的 Layout Shift
④ 渲染 Markdown / 代码高亮要在流式过程中增量处理，炸了会卡
```

---

## 8. RAG

### 流程

```
文档切分 → embedding → 向量库 → 召回 → rerank → 拼 prompt
```

### 优化点

| 环节 | 手段 |
|---|---|
| **切分** | 按语义/标题切、设重叠；**父子文档**（小块做检索、大块喂模型） |
| **检索** | **混合检索**（向量 + BM25）、Query 改写（HyDE / 多查询扩展） |
| **排序** | 加 **rerank 模型**（召回 50 条 → rerank 选 5 条） |

### 两个真实难点

- **切分粒度的权衡**：太小丢上下文、太大降召回精度
- **"检索到了但模型没用/用错"**——这是幻觉的另一种形态，比"没检索到"更难发现。解法：强制引用出处、把证据结构化喂进去、要求"只用给定材料回答"

---

## 9. 评测与幻觉治理

### 评测分层

```
① 单步：工具调用准确率（选对工具 + 参数正确）
② 端到端：任务完成率、答案正确率
③ 人工抽检：兜底，发现自动化指标看不见的问题
```

**LLM-as-judge 的两个偏差要主动提**：**位置偏差**（偏好靠前的选项）和**长度偏差**（偏好长回答）。缓解：交换位置重复判、给评分标准、和人工标注做校准。

**必须有回归测试集**——否则"优化"无法证明有效。

### 幻觉治理（发现 + 衡量）

```
发现：抽样人工看 / 用规则检测（引用不存在的数据源 / 数字对不上原始数据）
抑制：RAG 约束（只用给定材料）、强制引用出处、结构化输出、
      自我一致性（多次采样投票）、约束解码
衡量：优化后要有指标对比（幻觉率从多少降到多少，用什么方式测的）
```

**关键**：被问"怎么降低幻觉"时，一定要答**"怎么衡量"**——只讲手段不讲度量会被认为没做过生产优化。

---

## 10. 限流与成本控制

### 为什么 LLM 场景的令牌桶要"加权 + 预扣"

```
令牌桶的"令牌" = 请求配额（通常一个请求扣 1）
LLM 的 token  = 实际文本 token 数（几百到几万）
→ 用"每请求扣 1"来限 TPM 是错的
```

**正确做法：加权令牌桶 + 预扣结算**

```
① 请求前【预估】消耗（prompt 长度 + max_tokens）
② 按预估值【预扣】令牌
③ 拿到响应后按【实际用量】退差额（预扣 1000、实际 300 → 退 700）
④ 令牌不够 → 排队或降级（换小模型 / 截断上下文）
```

**为什么必须预扣**：发送前不可能知道真实消耗，只能按 max_tokens 预扣——否则并发请求都"以为够"→ 超发打爆上游配额。

### 其他控制手段

```
· token 预算：单请求上限、单会话上限
· 模型分级：简单任务走小模型、复杂任务才上大模型
· prompt 缓存：重复的系统提示/上下文可缓存（上游支持的话）
· 降级策略：熔断后走缓存 / 走小模型 / 返回兜底
```

### 令牌桶实现要点（含两个易错点）

```go
func (b *TokenBucket) Allow(n float64) bool {
	b.mu.Lock()
	defer b.mu.Unlock()
	now := time.Now()
	// ① 惰性补令牌：按真实流逝时间算，上限是桶容量（不要用定时器）
	b.tokens = math.Min(b.capacity, b.tokens+now.Sub(b.last).Seconds()*b.rate)
	// ② ★ last 必须【每次都更新】，无论放行还是拒绝
	//    否则被拒绝的请求不推进时间 → 下次把已用掉的时间重复计算 → 限流失效
	b.last = now
	if b.tokens >= n {
		b.tokens -= n
		return true
	}
	return false
}
```

**分布式场景必须 Redis + Lua**：读-算-判断-写四步要原子，否则并发下多个请求同时读到"够"，各自扣减 → **超发**。另外 key 必须设过期（否则每个用户一个 key 常驻内存），时间戳建议用 Redis 的 `TIME`（客户端时钟漂移会让令牌算错）。

---

## 11. 可观测性（trace 什么）

```
每一步都要 trace：
  · prompt 全文（含注入了哪些上下文/工具）
  · 模型输出（包括工具调用的参数）
  · 工具调用结果/耗时/错误
  · token 用量（分 input/output）
  · 每一轮耗时

没有这个，出问题只能靠猜——"为什么这次答得慢/贵/错"都答不上来。
```

---

## 汇总：这条简历的答题纪律

```
① 每个点都要落到【数字】和【真实故障】，不能只有名词
② 被问"为什么这样设计"要给【取舍】——为什么不选另一个
③ 被问"怎么优化"必须答【怎么衡量】——否则显得没做过生产
④ 主动说【边界】：什么场景不适用、什么情况下我们会拆/不拆
```
