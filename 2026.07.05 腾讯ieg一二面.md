# 2026.07.05 腾讯 IEG 一二面

## 一面

### 1. GMP 原理

- **G (Goroutine)**：用户态协程，本质是一个结构体，保存了栈、程序计数器、状态等执行上下文，是"要执行的任务"的封装。
- **M (Machine)**：操作系统线程，真正在CPU上跑代码的实体，和内核线程一一对应。M必须绑定一个P才能执行G。
- **P (Processor)**：逻辑处理器，是G和M之间的中间层，持有一个本地可运行队列（Local Run Queue）。P的数量由`GOMAXPROCS`决定，决定了同一时刻最多有多少个M能并发执行Go代码。
- **调度流程**：新建的G优先放入当前P的本地队列（满了则放入全局队列 Global Run Queue）；M绑定P后先从本地队列取G执行，本地队列空了就去全局队列拿，全局队列也空就触发 **work-stealing**：随机挑一个别的P，偷它本地队列一半的G过来执行。

### 2. 阻塞时的 GMP 动作

分两种阻塞：

- **用户态阻塞**（channel、mutex、waitgroup等）：G被挂起，状态变为`waiting`，直接让出，M不阻塞，会去P队列里继续取别的G执行；条件满足后G变回`runnable`重新入队。
- **系统调用阻塞**：
  - **网络I/O**：Go runtime用netpoller（底层epoll/kqueue）把socket设成非阻塞，G阻塞在网络读写时不会真的阻塞M，而是被挂起注册进netpoller，M可以去跑别的G，等事件就绪后被唤醒重新调度——所以网络阻塞不会导致P/M解绑。
  - **真正的阻塞系统调用**（文件I/O、cgo调用等）：M会跟着阻塞在内核态，这时runtime会把P从这个M上**解绑（handoff）**，转交给其他空闲的M（或新建M）去继续执行该P本地队列里的G，避免一个阻塞的G拖垮一整个P。系统调用返回后，G尝试重新获取P，抢不到就丢进全局队列等待调度；后台的`sysmon`线程会监控系统调用耗时，超时会触发这个解绑动作。

### 3. Linux 服务器资源使用情况怎么看

- **CPU**：`top`/`htop`看实时占用，`uptime`看负载(load average)，`vmstat 1`看CPU/内存/IO趋势，`mpstat -P ALL`看每个核心。
- **内存**：`free -h`看总量/已用/缓存/swap，`/proc/meminfo`看详细项。
- **磁盘IO**：`iostat -x 1`看吞吐和利用率(%util)，`iotop`定位到具体是哪个进程占用IO。
- **磁盘空间**：`df -h`看分区，`du -sh *`看目录占用。
- **网络**：`sar -n DEV 1`看流量，`iftop`/`nload`实时看带宽占用。
- **进程级**：`ps aux --sort=-%cpu`，`pidstat -p <pid>`看单进程CPU/IO明细。
- **综合工具**：`dstat`一屏汇总CPU/内存/磁盘/网络；`sar`可以看历史数据（依赖sysstat）。

### 4. grep 怎么看文件

- 基本：`grep "pattern" file`
- `-r`/`-R`：递归搜索目录
- `-i`：忽略大小写
- `-n`：显示行号
- `-v`：反向匹配（排除某模式）
- `-c`：统计匹配行数
- `-A n`/`-B n`/`-C n`：显示匹配行后/前/前后n行上下文
- `-E`：扩展正则（等价于`egrep`）
- `-w`：整词匹配
- `-l`：只列出匹配到的文件名
- `--include="*.go"`/`--exclude`：按文件类型过滤
- 常见组合：`tail -f app.log | grep --line-buffered "ERROR"` 实时过滤日志；`grep -c "ERROR" app.log` 统计出错次数。

### 5. sync.Pool

- **作用**：临时对象池，复用短生命周期、创建成本高的对象（如`bytes.Buffer`、临时结构体），减轻GC压力。
- **原理**：
  - 每个P对应一个本地缓存，分为`private`（一个对象）和`shared`（一个列表）。`Get`时先取本地`private`，没有再取本地`shared`，还没有就尝试从别的P的`shared`里偷，最后实在没有就调用用户提供的`New`函数创建。
  - `Put`时对象放回本地`private`或`shared`。
  - **对象不是永久缓存**：每次GC会清空Pool（Go 1.13起引入了victim cache二级缓存，本轮清空前先挪到victim，如果下一轮还没人用victim才会被真正清空，减少性能抖动）。
  - 因此sync.Pool只适合"短期复用"，不能当持久化缓存用，也不保证Put进去的对象一定还能Get到。

---

## 二面

### 1. GC 三色标记回收原理

- **三色抽象**：白色（未访问，本轮结束后当垃圾清除）/ 灰色（已访问但引用的子对象还没扫描完）/ 黑色（已访问且子对象也都扫描完）。
- **标记流程**：
  1. 初始全部对象为白色；
  2. 从root（栈、全局变量、寄存器）出发，可达对象标记为灰色，放入待处理队列；
  3. 不断从灰色队列取对象，扫描其引用的子对象（白色则标记为灰色入队），自身标记为黑色；
  4. 灰色队列清空即标记结束，黑色是存活对象，白色是垃圾，进入清除阶段回收。
- **三色不变式**（并发标记时用户代码仍在跑，需要保证正确性）：
  - 强三色不变式：黑色对象不能直接引用白色对象；
  - 弱三色不变式：黑色对象可以引用白色对象，但这个白色对象必须还存在一条从某个灰色对象出发的可达路径。
  - 如果并发过程中破坏了不变式（比如黑色对象新增了一个指向"孤立"白色对象的引用），就会**漏标**，导致存活对象被误清除——这正是需要写屏障来兜底的原因。

### 2. 写混合屏障（Hybrid Write Barrier）

- **背景**：Go 1.8前用插入屏障（Dijkstra），需要在GC开始时STW去扫描所有goroutine的栈以保证正确性，这个STW时长和goroutine数量、栈大小相关，不可控。
- **Go 1.8的混合写屏障规则**：
  1. GC开始时**不再STW扫栈**；每条 goroutine 的栈在并发标记期被扫**一次**（在它自己的安全点），扫黑的栈本轮不再重扫，之后压到"已扫描栈"上的对象视作黑色；
  2. 堆上指针赋值操作时，同时做两件事：
     - 被覆盖删除的旧对象标记为灰色（类似删除屏障，防止栈上已有引用被覆盖后原对象被误删）；
     - 新写入的对象标记为灰色（类似插入屏障，防止新引用的对象被漏标）。
- **效果**：既省去了对栈的STW重新扫描（大幅降低STW时间），又能保证并发标记的正确性，代价是每次指针赋值多一点点写屏障的开销。

### 2.1 追问：一轮 GC 的完整时序（两个 STW + 栈懒扫 + 位图对调）

```
STW #1（开场，亚毫秒级）
  ├─ sweep termination：收尾上一轮没扫完的 span
  └─ mark setup：打开写屏障、启动 mutator assist
       ↓ 放开所有 goroutine
并发标记（GC worker 约占 25% CPU，业务照常跑）
  ├─ 灰色队列非空：取出对象 → 引用的白对象 shade 入队
  ├─ 堆（含全局）指针写 → 混合屏障：旧值、新值都标灰
  ├─ 每条 goroutine 的栈：被抢占/阻塞到安全点时扫【一次】
  ├─ 标记期间新分配的堆对象 → 生而黑
  └─ 已扫描的栈上新压栈的对象 → 视作黑
       ↓ 灰队列空 + 所有栈扫完
STW #2（mark termination，亚毫秒级）
  ├─ 收尾 drain、flush 写屏障缓冲（wbBuf）
  └─ 关写屏障
       ↓
并发清扫（不 STW：后台 sweeper + 分配时惰性清扫）
  └─ span 把 gcmarkBits ↔ allocBits 位图对调
     → 没标到的格子自动成为自由内存，"下一轮人人皆白"零成本成立
```

- **开场 STW 不扫栈**，结尾 STW 是兜底（补扫卡在不可抢占区域的栈）。
- **没有"STW 标白"这一步**——下一轮的白靠位图对调实现，不需要全局清零。
- 清扫是**并发且惰性**的，不在 STW 里做。

### 2.2 追问：混合屏障的"四条规则"到底是哪四条？

写屏障对旧值、新值各算一条，加上两条"生而为黑"，正好四条：

| # | 规则 | 精确表述 |
|---|---|---|
| 1 | 删除屏障（旧值灰） | **堆**上指针槽被覆盖时，原来指向的那个对象 shade 成灰 |
| 2 | 插入屏障（新值灰） | **堆**上指针槽写入时，新指向的对象 shade 成灰 |
| 3 | 堆分配即黑 | 标记期间在堆上新分配的对象，出生就是黑 |
| 4 | 栈分配即黑 | **已扫描变黑的栈**上，之后新创建/压栈的对象视作黑 |

两个容易说错的精度点：

- 规则 1 的"旧对象"指**被覆盖删除的那个**（slot 原来指向谁），防的是"指针从堆搬家到栈后，原槽位被覆盖"这条漏标路径。
- 规则 4 只对**已扫描的栈**成立；未扫描的栈不需要它——反正会被完整扫一遍。
- 屏障只挂在**堆（含全局变量）的指针写**上，**栈写没有屏障**——规则 3、4 存在的原因正是栈写无屏障。

为什么恰好四条就闭环（弱三色不变式恒成立）：

```
黑栈新增一个引用，来源只有三种：
  a. 指向新分配对象     → 规则 3/4：生而为黑
  b. 从堆/全局加载过来   → 要么该堆位置不动，正常图遍历会标到它；
                          要么后来被覆盖，规则 1 已把旧值 shade
  c. 从未扫描的栈传过来  → 那个栈迟早被扫，扫到就 shade
黑栈【丢】引用 → 只可能产生浮动垃圾（多标），不漏标，无害
```

补充细节：官方 proposal 里规则 2 原本带条件"当前栈还是灰的才 shade 新值"，**实际 1.8 发布的实现简化成无条件双 shade**（wbBuf 把新旧值都进缓冲，flush 时一起标）——提这个能证明看的是实现不是二手博客。

### 2.3 追问：栈是什么时候被扫的？是等它阻塞吗？

**不用等阻塞——阻塞只是安全点的一种**，运行中的 G 会被 runtime 主动抢到安全点：

```
停止路径                                 扫描怎么发生
─────────────────────────────────    ──────────────────────────
① 阻塞/让出（channel、锁、select、      已经停了 → 栈不再变化，
   sleep、gopark）                     直接扫（自己扫或 worker 挂起扫）
② 进系统调用                            不在用户态跑代码，栈冻结 → 可扫
③ 正在运行                              主动抢占：
                                        ├ 协作式：函数序言的 morestack 检查点
                                        └ 异步式(1.14+)：向线程发信号 SIGURG
④ 卡在不可抢占区域（拿 runtime 锁、      拖到 mark termination STW 里兜底扫
   屏障缓冲 flush 中）
```

- **谁来扫**：两条路径都有——G 在停进安全点的路上发现本轮没扫过（`gcscanvalid`），可以自己扫自己；或后台标记 worker 通过 `scang` + `suspendG` 挂起它再扫。每轮每个栈只扫一次。
- **为什么必须停着扫**：运行中的 G 栈帧在变、栈还可能增长搬移，扫活动中的栈等于追移动目标。
- **Go 1.14 之前**只有协作式抢占，一个 `for {}` 紧密循环就能让 STW 无限等它到安全点——这是当时著名的尾延迟来源；异步抢占（信号）就是为此引入的。

### 3. Linux 怎么看服务器 TCP 连接数

- `ss -s`：直接看TCP各状态连接数的汇总统计。
- `ss -ant | awk '{print $1}' | sort | uniq -c`：按状态统计连接数。
- `netstat -ant | grep ESTABLISHED | wc -l`：统计已建立的连接数（netstat较老，性能不如ss，现代系统建议用ss）。
- `lsof -i :端口号`：看某个端口上的连接明细。
- `cat /proc/net/tcp`：查看内核记录的原始连接信息（需要自己解析十六进制状态码）。

### 4. 全栈角度排查"点击后没反应"

按用户操作路径从前到后逐层排查：

- **前端页面层**：F12打开Console看有没有JS报错中断了逻辑；检查事件绑定是否生效（`disabled`、loading锁死、`stopPropagation`拦截、CSS `pointer-events:none`或被遮罩层挡住）。
- **网络请求层**：Network面板看点击后有没有发出请求、URL/参数对不对、请求是否pending卡住、状态码是什么；检查是否超时、DNS异常、CORS跨域被拦截。
- **网关/后端层**：看Nginx/网关的access log、error log确认请求有没有到达后端及返回码；看后端服务日志确认是否进入对应接口逻辑、有没有异常堆栈；微服务场景配合链路追踪（Jaeger/Zipkin/SkyWalking）定位卡在哪个服务。
- **数据/依赖层**：检查数据库/缓存/下游服务是否超时、慢查询、连接池耗尽。
- **监控兜底**：APM监控（QPS/延迟/错误率）+ 前端错误监控（如Sentry）看对应时间点是否有异常。
- **总结思路**：点击 → 前端事件/请求 → 网络传输 → 网关/后端服务 → 数据依赖，逐层排查，结合日志+链路追踪+监控定位具体卡在哪一层。

### 5. K8s 面试常考点（通用补充，具体考题待回忆细化）

- 核心对象：Pod、Deployment、Service、ConfigMap/Secret、Namespace、Ingress
- 调度：kube-scheduler怎么选节点（亲和性/反亲和性、资源request/limit、taint/toleration）
- 网络：Pod间通信模型、Service的ClusterIP/NodePort/LoadBalancer、kube-proxy的iptables/ipvs模式、CNI插件
- 存储：PV/PVC、StorageClass动态供给
- 健康检查：`livenessProbe`（失败重启容器）vs `readinessProbe`（失败从Service的Endpoint移除但不重启）
- 滚动更新/回滚：`RollingUpdate`策略、`maxSurge`/`maxUnavailable`
- HPA自动扩缩容原理
- etcd在集群中的作用：存储所有集群状态数据
- controller-manager的控制循环（reconcile loop）机制

### 6. K8s 部署 vs 手动部署对比（面试官问"最反感K8s部署的点"）

- **K8s相对手动部署的优势**：Pod自愈/自动重启、HPA弹性伸缩、声明式yaml避免配置漂移、Service/Endpoint内建服务发现和负载均衡、滚动更新和回滚方便。
- **K8s让人"反感"/劣势的点**（面试官想听到的对比）：
  - **学习成本高、概念复杂**：Pod/Deployment/Service/Ingress/PV/PVC/RBAC一大堆，小规模简单项目属于过度设计；
  - **排查链路变长**：出问题要一层层查（Pod日志 → `kubectl describe`看事件 → NetworkPolicy → Service/Endpoint → Ingress → 节点资源），比直接登录机器看进程/日志繁琐得多；
  - **额外资源开销**：控制平面(etcd/apiserver/scheduler/controller-manager)本身，以及sidecar（如Istio的envoy）都会额外吃资源，小集群性价比低；
  - **网络/存储抽象带来的不确定性**：CNI、iptables规则复杂，网络抖动/丢包时定位困难；PV/PVC有时会遇到挂载延迟、权限问题等边缘case；
  - **YAML地狱**：一个简单服务可能要写Deployment+Service+ConfigMap+Ingress+HPA好几个文件，远比手动部署一个二进制+启动脚本繁琐；
  - **版本升级/兼容成本**：K8s版本升级、CRD/API废弃迁移也是持续的运维负担；
  - **对精细控制底层资源场景不友好**：比如需要固定IP、特殊内核参数、GPU拓扑等场景，K8s的抽象反而成为限制。
- **总结话术**：K8s适合大规模、多团队、需要标准化自动化运维的场景；小规模/单体应用/追求极简部署时，手动部署+轻量工具（systemd、supervisor、ansible）反而更直接、排查更快、资源开销更小。回答"最反感的点"可以落在：**抽象层次多导致问题定位链路变长，以及资源开销和学习曲线相对手动部署更高**。

### 7. Go reflect 包 vs 泛型 对比

- **reflect（反射）**：
  - **运行时机制**。Go中interface底层是`(类型指针, 数据指针)`的二元组，reflect通过读取这个类型信息，在运行时动态获取/修改变量的类型(`reflect.Type`)和值(`reflect.Value`)，可以遍历struct字段、读取tag、动态调用方法等。
  - **优点**：灵活，能处理"编译期完全不知道具体类型"的场景，比如`encoding/json`序列化任意结构体、ORM框架读取struct tag映射数据库字段、依赖注入框架。
  - **缺点**：
    - 性能开销大：走接口装箱/类型断言，是间接调用，编译器无法内联优化；
    - 类型不安全：类型错误只能在运行时通过panic暴露，编译期发现不了；
    - API繁琐、代码可读性差（`Elem()`/`Kind()`/`NumField()`一堆链式调用）。

- **泛型（Generics，Go 1.18引入）**：
  - **编译期机制**。通过类型参数（type parameter）让函数/类型对多种具体类型工作，同时保持类型安全。
  - **实现原理**：Go泛型走的是"字典传递（dictionary-passing）+ 部分单态化（GCShape stenciling）"的折中方案——不是像C++模板那样为每个具体类型都生成一份完整代码（会导致代码膨胀），而是对形状(shape)相同的类型共享一份代码，运行时通过传递一个包含类型信息/方法集的"字典"来决定具体操作。
  - **优点**：
    - 编译期类型检查，类型错误编译时就能发现；
    - 性能明显好于reflect（没有运行时接口断言的开销）；
    - 消除重复代码（之前只能靠`interface{}`+反射，或者对每个类型手写一份）。
  - **局限**：
    - 只能通过约束（constraints，本质是接口）限制类型的方法集/操作符，不能像C++模板做复杂的编译期计算；
    - 不支持"泛型方法"（方法本身不能有独立的类型参数，只有函数和类型可以带类型参数）；
    - 无法替代reflect在"完全未知结构"场景下的自省能力（比如动态解析任意JSON、读取任意struct的tag），这类场景依然要用反射。

- **核心对比**：

  | 维度 | reflect | 泛型 |
  |---|---|---|
  | 生效时机 | 运行时 | 编译期 |
  | 类型安全 | 运行时才报错(panic) | 编译期检查 |
  | 性能 | 慢（装箱+类型断言，无法内联） | 接近原生（远好于reflect） |
  | 适用场景 | 完全未知类型的结构自省（JSON/ORM/DI框架） | 已知一组类型需要同一套逻辑（容器、通用算法Map/Filter/Reduce） |

  两者不是互相替代关系：泛型解决的是"一套逻辑支持多类型"的复用问题，reflect解决的是"运行时动态探查未知结构"的问题，标准库里两者经常配合使用（比如某些通用容器用泛型写外层API，内部涉及序列化的地方仍然依赖reflect）。

## 待复盘 / TODO

- [ ] 二面K8s具体考题内容需要补充回忆细化
- [ ] 手写代码验证一下sync.Pool的victim cache行为
- [ ] 实际用ss/netstat在服务器上跑一遍练习命令
