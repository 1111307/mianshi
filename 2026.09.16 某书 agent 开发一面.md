# 2026.09.16 某书 agent 开发一面

> 面试：2026.9.16，某书 agent 开发一面，约 45 分钟，9 道题（8 道项目深挖 + 1 道手撕 LRU）。
> 简历对应条目：车辆数据平台（Redis 权威源 + 进程内 LRU 缓存）、ETL 攒批落库链路、售后工单系统、数采链路看板 agent。
> 本文按「**面试官原话 → 我当时的答法 → 缺口 → 更好的答法 → 追问预案**」逐题复盘，技术点均标注源码出处与版本。

---

## 0. 这场面试的主线：面试官在拆「我做了性能优化」这个前提

九道题不是九个孤立考点，是**一条链**：

```
Q1 为什么用 go-lru，调研过其他的吗 ← 验选型有没有依据
        ↓ 我答"用了 expirable，为了降低网络开销和往返时延"，并主动提了高峰期 QPS 5-6k
Q2 你自己也说了高峰期 redis qps 5-6k，这对 redis 来说压力也不大啊？
        ↓ 我给的"理由"被当场拆掉：5-6k 恰恰证明 Redis 没压力
Q3 你说为了降低网络开销和往返时延？你们 redis 时延 p95 多少？
        ↓ 退守到"降延迟"，但拿不出任何数字 → 整条优化动机链断在这里
Q4 你们有注意缓存一致性的问题吗？ ← 代价那笔账（我没算过）
        ↓
Q5 redis 内存淘汰策略？过期删除？只能惰性删除？ ← 从我亲口说的"没有 TTL"反推我的架构
```

**这场面试的核心考点不是"你知不知道 LRU / Redis 淘汰策略"，是"你的优化有没有被度量过"。**

三条可迁移的教训：

1. **主动交代的理由会成为被拆的靶子。** 我把"QPS 5-6k"当作加本地缓存的论据说出来，面试官立刻用它反证 Redis 没压力——**论据要能互相支撑，不能互相拆台**。
2. **"降延迟"这类说法必须带数字。** 说降延迟却报不出 p95，等于承认这个优化不是数据驱动加上去的。
3. **架构自述里的每一句都会被反推。** 我说"Redis 没有 TTL"，面试官下一题就问淘汰策略——因为**没有 TTL 意味着 volatile-\* 对我全是死策略**（见 Q5），这是我自己没意识到的洞。

---

## 1. Q1：你在内存中做 LRU 缓存，为什么用 go-lru 这个包，有调研其他的吗？

### 我当时的答法

只落到「用了 hashicorp/golang-lru 的 **expirable** 子包（带 TTL + 后台扫描 goroutine）」，理由挂在"降低网络开销和往返时延"上，并主动提了"高峰期 Redis QPS 5-6k"。**没有任何横向调研的结论**（ristretto / go-cache / bigcache 一个都没提）——这正是"有调研其他的吗"这一问的由来。

### 缺口

答"它有 TTL 所以用它"是**功能描述，不是选型**。缺三层：

- **没有对比维度**（容量上限 / 淘汰算法 / 命中率 / GC 压力 / 维护活跃度）
- **说不出源码级的坑**，面试官无法判断你是真读过还是随手挑了个 star 最多的包
- **理由没量化**，直接导致 Q2/Q3 被打穿

### 横向对比（该背的表）

| 库 | 淘汰算法 | 容量上限 | TTL | 特点 / 代价 |
|---|---|---|---|---|
| **hashicorp/golang-lru v2** | LRU（`simplelru`/`lru`），另有 `expirable`（LRU+TTL）、`arc`（ARC 自适应） | 有 | **仅 expirable 提供** | 零依赖、泛型、约 150 行可读完；`lru.Cache` 用 `RWMutex`、`expirable.LRU` 用 `Mutex` |
| **dgraph-io/ristretto** | **TinyLFU 准入 + cost-based 采样淘汰** | 有 | **不内建**（需 `ttlcache` 包装） | 抗扫描、命中率更高；要自己接指标，API 更重 |
| **patrickmn/go-cache** | **无淘汰算法** | **无** | 有 | 只有 janitor 定时清过期，容量不受控 → 不适合做有界缓存 |
| **eko/gocache** | 委托后端 | 委托后端 | 有 | 统一抽象层（memory/redis/…），自身不含算法 |
| **bigcache / freecache** | FIFO / 近似 LRU | 有 | 有 | 存 `[]byte`、**规避 GC**，面向百万级大容量；不支持任意类型 |

**选型话术**：

> 按四个维度选：**容量上限、TTL、泛型、源码可读性**。expirable 一包给全 LRU + TTL + 后台清理，零依赖、150 行能读完，所以我们选了它。如果**命中率**是首要指标会换 ristretto 的 TinyLFU；go-cache 没有容量上限，直接排除；bigcache/freecache 面向大容量无 GC 压力场景，我们工作集只有几千个 VIN，用不上。

### expirable 的三个源码级坑（**必须标版本**）

> ⚠️ 引用这个包**一定要标版本号**，不同版本行为不同。以下全部核对于 **v2.0.7**（当前最新发布版），源码 `github.com/hashicorp/golang-lru/v2/expirable/expirable_lru.go`。

**坑 1：`Add` 已存在的 key 会【续期 TTL】**

```go
// expirable_lru.go:126-133
if ent, ok := c.items[key]; ok {
    c.evictList.MoveToFront(ent)
    c.removeFromBucket(ent) // remove the entry from its current bucket as expiresAt is renewed
    ent.Value = value
    ent.ExpiresAt = now.Add(c.ttl)   // ← :130 续期
    c.addToBucket(ent)
    return false
}
```

**后果**：如果实现成「读命中 → 回写刷新」的写法，这个 key **永远不会过期**；而且它同时被 `MoveToFront` 顶成 MRU，**连容量淘汰这条唯一出路也几乎不触发**——比"只是续期"还严重。

**坑 2：`size = 0` 会【静默关掉 LRU 机制】**

```go
// expirable_lru.go:49（NewLRU 文档注释）
// Size parameter set to 0 makes cache of unlimited size, e.g. turns LRU mechanism off.
// expirable_lru.go:55-57
if size < 0 { size = 0 }
// expirable_lru.go:140
evict := c.size > 0 && c.evictList.Length() > c.size   // ← size=0 时 evict 恒 false
```

传 0（或负数）**不报错、不 panic**，只是从"有界缓存"悄悄变成"无界 map"。

**坑 3：`ttl <= 0` 时【TTL 变成 10 年，且根本不启动清理 goroutine】**

```go
// expirable_lru.go:41
const noEvictionTTL = time.Hour * 24 * 365 * 10   // 10 年
// expirable_lru.go:58-60
if ttl <= 0 { ttl = noEvictionTTL }
// expirable_lru.go:81-94
if res.ttl != noEvictionTTL {
    go func(done <-chan struct{}) {
        ticker := time.NewTicker(res.ttl / numBuckets)   // ← :83 周期 = ttl/100
        ...
    }(res.done)
}
```

传 `ttl = 0` 想表达"不过期" → 实际是"**10 年后过期**"，且**清理 goroutine 完全不启动**。

**另外三个易错点**：

- **`Contains` 不判过期**（`:166-171`）—— 只用它判存在会拿到已过期的 key
- **`Keys()` 不滤过期**（`:223-231`）但 **`Values()` 滤**（`:235-249`，`:242` 处 `if now.After(ent.ExpiresAt) { continue }`）—— 两个方法语义不一致
- **`Close()` 在 v2.0.7 里不存在**——被整段注释掉了（`:277-287`）。`:78-80` 的原注释写得很直白：

  ```go
  // Important: done channel is never closed, so deleteExpired() goroutine will never exit,
  // it's decided to add functionality to close it in the version later than v2.
  ```

  → **v2.0.7 下每个 LRU 实例的清理 goroutine 随进程常驻，永不退出**。上游 main 分支已重写该包（加了可用的 `Close()`、`cleanupStopped`、回调移出锁外）。

### ⚠️ 一条需要澄清的常见误解：「生效 TTL 是 [ttl, ttl+ttl/100]」

**这是错的。** ticker 周期 `ttl/100` 属实（`:45` + `:83`，`numBuckets = 100`），但它只决定**内部 map 里的条目何时被物理删除（释放内存）**，不决定可见性。

读路径是**精确判定**：

```go
// Get  :155
if time.Now().After(ent.ExpiresAt) { return value, false }
// Peek :181
if time.Now().After(ent.ExpiresAt) { return value, false }
```

→ **对调用方而言生效 TTL 恰好等于 `ttl`**，不存在 +ttl/100 的上浮。`[ttl, ttl+ttl/100]` 描述的是内部清理时刻，不是可见期。

（`deleteExpired` 还有个细节：`:308-323` 它会先算 `timeUntil(buckets[idx].newestEntry)`，若还没到就**不持锁地 `Sleep`**（`:313-317` unlock → sleep → lock），再整桶删除、推进 `nextCleanupBucket`。`addToBucket` 的桶选择是 `(numBuckets + nextCleanupBucket - 1) % numBuckets`（`:327`），即"下一个要清理的那个桶"。）

### 追问预案

| 追问 | 怎么答 |
|---|---|
| ristretto 的 TinyLFU 比 LRU 命中率高多少，有实测吗 | 不要编数字。老实说"没有实测，我们工作集小、访问分布没有明显扫描型流量，LRU 够用；换 ristretto 要自己接 TTL 和指标，收益不明确" |
| expirable 每个实例一个清理 goroutine，你们部署多少 pod，算泄漏吗 | **v2.0.7 下它确实永不退出**（`:78-80` 源码注释自认）。但每实例只有 1 个 goroutine、且随 Cache 生命周期常驻，不是"泄漏"；真正要注意的是**别在热路径反复 NewLRU** |
| 车辆信息变更时你怎么失效本地缓存，多实例怎么通知 | 见 Q4 |

---

## 2. Q2：你自己也说了高峰期 redis qps 5-6k，这对 redis 来说压力也不大啊？

### 面试官在做什么

**拆前提。** 不是问"你在做什么"，是问"你给的因撑不起你的果"——借"5-6k 对 Redis 不算高"直接否掉你加本地缓存的前提。

### 我当时的答法

**没有正面接住这个质疑，也没有承认前提成立**，而是回到原来的优化动机上——把"高峰 5-6k QPS"和"减少往返时延"并列当作两条理由。全程没给任何 RTT 数字。

### 缺口

**两个论据互相拆台**：

- 如果理由是"QPS 高" → 5-6k 恰恰证明不高，**等于自己否掉自己**
- 如果理由是"降时延" → 必须有 RTT 数字，而我一个都没给

结果面试官下一句直接问 p95（Q3），整条链在这里就断了。

**正确姿势：先认前提，再换赛道，而不是复述理由硬撑。**

### 正确的答法

> 5-6k 只占 Redis 单实例（命令单线程，简单 GET 在 **10 万 QPS 量级**）的 **5%-6%**，拿吞吐当理由是站不住的，这点我认。
>
> 本地缓存真正省的是**尾延迟**：
> - 本地命中：加锁 + map 查表 + 链表前移，**约 100ns 量级**（Linux 无竞争下；高竞争时会退化到微秒级）
> - 同机房 Redis GET：中位数约 **0.2-0.5ms**，P99 在 1ms 内
> - **两者差约三个数量级**，而这个量级差才是本地缓存唯一硬的理由
>
> 另外 Redis 平均负载再低，**p99 仍会被 BGSAVE fork、AOF rewrite、大 key 删除/过期、慢查询拉到数 ms** —— 尾延迟不能由平均 QPS 推出来。
>
> 代价是接受秒级不一致（见 Q4）。

> ⚠️ **别把"10 万 QPS"说成官方基准实测**。它出自 pipelining 文档里一个假设句的让步分句（"even if the server is able to process 100k requests per second…"），是量级参考，不是 bench 结论。被追问口径时要说清。

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 既然为了降时延，为什么不做连接池 / pipeline，或本地起个 Redis 代理 | 连接池解决的是**建连**开销，不是单次 RTT；pipeline 要求一次发多条、有批处理语义，读路径是单 key 同步查，用不上；本地代理仍多一跳。**只有本进程内存能消掉那一跳** |
| 你本地缓存命中率多少，怎么证明省下了时延 | 见下条——这题我当时答不出来，**要承认并给出补法**：加命中率埋点 + 接口 P99 前后对比 |
| 省一次 RTT 换来一致性风险，这笔账算过吗 | 见 Q4 |

---

## 3. Q3：你说为了降低网络开销和往返时延？你们 redis 时延 p95 多少？

### 面试官在做什么

考"**这个优化到底量没量过**"。说降延迟却报不出 p95，等于承认本地缓存不是被数据驱动加上去的，是**照抄最佳实践**。

配合 Q2：5-6k QPS 对 Redis 毫无压力，所以"减压"和"降延迟"两个理由面试官一个都不认。

### 我当时的答法

给的是**定性理由**——"为了降低网络开销和往返时延"，**拿不出任何 p95/p99 数字**。也没提本地缓存命中率、没有接口层延迟的前后对比。

### 缺口（四条）

1. **答不出 p95 = 没建立延迟可观测性。** p95/p99 是"慢的那 5%/1% 有多慢"，均值会把长尾抹平，而**用户体验和超时率恰恰由尾部决定**
2. **"减压"站不住**：5-6k 只占单实例能力的约 5%
3. **省的是 RTT，不是 Redis 服务时间**：本地命中是加锁 + 查表 + 指针重连（百纳秒级），同机房 RTT 是 0.2-0.5ms，**差三个数量级**
4. **说不出收益就更说不出代价** → Q4 缓存一致性必然被追着打

### 正确的答法

> **没单独埋 Redis 的 p95，只看了接口 P99 和 slowlog —— 这是缺口，我先认。**
>
> 量级上：同机房 Redis GET 中位数约 0.2-0.5ms，P99 在 1ms 内；本地 LRU 命中约 100ns 量级，**差三个数量级**。
>
> 5-6k QPS 确实压不垮 Redis。**我做本地缓存图的是把接口 P99 钉住、以及 Redis 抖动（fork / 大 key 过期 / 慢查询）时兜底，不是省吞吐。**

**关键是"先认缺口再给量级"** —— 承认没量过比硬编一个数字安全得多。硬编 p95 一旦被追问口径（哪个接口、哪个分位、观察窗口、采样方式）就崩。

### 补充：Little's Law 的口径要小心

在同步阻塞调用链上，按稳态平均口径 `in-flight = QPS × 平均延迟`：`5500 × 0.3ms ≈ 2`。若用 P99 代入可粗估**尾部并发上界**：`5500 × 1ms ≈ 6`。

> ⚠️ 这是**容量估算的经验口径，不是严格的连接数推导**。Little's Law 是稳态平均恒等式，它描述"平均并发与速率/延迟的守恒关系"，**不描述放大机制**；代入 P99 还隐含"每连接每时刻只处理一个请求、无排队叠加"。面试口述时说"粗估上界"而不是"约需 6 个连接"。

---

## 4. Q4：你们有注意缓存一致性的问题吗？

### 我当时的答法

> 存的是车辆基本信息，**不太能发生改变**，当时也是基于这个原因设计的内存缓存。

### 缺口（三处会被打穿）

**① 混淆了 key 和 value**

VIN 作为主键确实不变，但「**所属项目**」「**车型**」是可变业务字段（车辆调拨、车型修正）——**变的正是 value，而我的论证只覆盖了 key**。

**② 「低变更频率」不是一致性模型**

变更频率只能**压低脏读被观测到的概率**，不提供任何一致性保证。

> ⚠️ 但这里要收一个精度（**这条我一开始也说过头了**）：
> **本地 expirable 层是有 TTL 的**，TTL 到期 Get/Peek 返回 miss → 回源 Redis 拿新值。
> 只要写路径把新值写进了 Redis（Redis 是唯一权威源，写就必须落 Redis），**本地脏读窗口就被本地 TTL 自动封顶（≤ 本地 TTL）**，不是"无上界"。
>
> **真正无上界的是 Redis 层本身**：一旦 Redis 里的值被写错/漏写，没有任何一层会自动纠正它。
>
> 会退化成"永久脏"的**触发条件**是两个，必须点明：
> - **(a) 本地读命中后 `Add` 回写续期了 TTL**（见 Q1 坑 1，且会被顶成 MRU，连容量淘汰都不触发）
> - **(b) 初始化时 TTL 传了 `<= 0`** → expirable 把 ttl 置为 10 年且**不启动清理 goroutine**（Q1 坑 3）

**③ 无 TTL + 手动失效 + 多实例 = 多副本下不可执行**

"手动触发"在单机是运维动作，**在多副本下根本不可执行**——你清不掉正在跑旧值的其他 Pod，而且**漏一次就永久脏**。

**④ 两层语义不一致**

本地 expirable **有** TTL，Redis 反而**没有** TTL。本地 TTL 到期只是重新回源，**回源拿到的可能还是同一个没被改的 Redis 值**——它根本不负责正确性。

### 正确的答法

> 有。而且"变更少"撑不住：
>
> **变的是 value 不是 key** —— VIN 不变，但项目归属、车型会改。
>
> **正解是写入口唯一化**：改完 Redis 之后走 Pub/Sub **广播失效 key**，各实例收到就删本地条目。注意播的是**失效（evict）不是新值**，与 Redis 官方的 invalidation 语义一致。
>
> **短 TTL 不是解** —— 算一下：20 个实例 × 2000 个 VIN 工作集 ÷ 10s ≈ **4000 QPS 的稳态回源上限**，跟现场说的 5-6k 峰值同量级。
> 不过要说清：**4000 QPS 对单实例 Redis 也不是瓶颈**（常规 10⁵ ops/s 量级），短 TTL 真正的问题是**它只封顶脏读窗口，不解决"写后立即一致"，也不解决"写路径不通知"**——代价是读放大和尾延迟，不是打满 Redis。

**Pub/Sub 方案的三个必须说的边界**（否则会被追着打）：

| 边界 | 事实 | 补法 |
|---|---|---|
| **投递语义是 at-most-once** | 官方原文：*"Redis' Pub/Sub exhibits at-most-once message delivery semantics"*；断线/重启期间的消息**永久丢失，不补发** | 必须有 TTL 兜底，或换 Streams / CDC |
| **订阅断连不易察觉** | 慢订阅者会被服务端踢掉（`client-output-buffer-limit pubsub 32mb 8mb 60`）；pub/sub 连接还**豁免 idle timeout**（`timeout.c` 跳过 `CLIENT_PUBSUB`），所以"连接活着但订阅死了"不会自曝 | 显式探活 + `PUBSUB NUMSUB` 监控 |
| **失效处理器必须幂等** | Redis 在失效表满时会发**伪失效**（`tracking-table-max-keys` 默认 1000000）；而 `delete` + 取 `max` 本身幂等可交换，重复投递无害 | 用 `max()` 语义而非覆盖写 |

**更省事的替代**：Redis 官方其实**明确不推荐自己用 Pub/Sub 搭失效总线**，推荐用 **CLIENT TRACKING（客户端缓存）**——服务端帮你追踪 key、断连时官方要求 flush 本地缓存。go-redis 从 **v9.22.0** 起有内置实现。

> 澄清一个常见误区：**官方 CLIENT TRACKING 的失效消息里没有 version/ETag 字段**，只有 key 名（或 FLUSH 时的 RESP NULL）。所以"要 version 就必须放弃官方机制、自建广播通道"——这个代价面试时要认。

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 某辆车的项目归属真要改，怎么让线上 20 个实例都换新值 | 写入口唯一化 + 广播失效；广播失败要补偿（重试 / outbox / CDC）。**漏一次就是脏到 TTL**，所以 TTL 兜底不能省 |
| 本地有 TTL、Redis 没有，哪一层负责正确性 | 本地 TTL 只负责**收敛上界**（脏读窗口 ≤ TTL），不负责正确性；正确性靠写路径唯一化 + 广播。Redis 无 TTL 是因为它是权威源，不能自己消失 |
| 不用 TTL 兜底，怎么保证写完那一刻没有实例还在读旧值 | 保证不了——这是**最终一致**。要写后立即一致就得读时校验版本或强制读主，代价是回源放大 |
| Pub/Sub 消息丢了怎么办 | 见上表：at-most-once + TTL 兜底 + 幂等处理器；要更强就 Streams（消费组 + ACK）或 CDC |

---

## 5. Q5：redis 内存淘汰策略知道吗？过期删除？只能惰性删除？

### 面试官在做什么

**不是让你数策略名，是拿八股当探针戳你的架构**：

> 你亲口说车辆 key **没有 TTL** —— 那 `volatile-*` 对你就是**死策略**，Redis 写满只会 OOM 报错。

答不出这层，说明缓存是抄来的不是设计的。"**只能惰性删除？**"就是在拆你"定时轮询"这个含糊前提。

### 我当时的答法

答了"带 TTL 的 key 的 LRU/LFU"+"不带 TTL 的 key 的 LRU/LFU"（即 volatile-lru/lfu + allkeys-lru/lfu，**共四类**）；过期删除答"惰性删除 + 定时轮询有 TTL 的 key"，随即被追问"只能惰性删除？"

### 缺口（三处）

**① 8 种 policy 只说了 4 种**

漏了 `noeviction`、`allkeys-random`、`volatile-random`、`volatile-ttl`。

**② 漏了 Redis 4.0 的异步删除（UNLINK / lazyfree-\*）**

**③ 最致命：我自己项目车辆 key 无 TTL，却完全没意识到 `volatile-*` 只从带 TTL 的 key 里淘汰** —— 全库无 TTL 时 `volatile-lru` **等价于 `noeviction`**，写满直接 OOM；而 Redis 又是权威源、没有下游数据库可回源，等于**策略选择直接绑死架构**。

### 淘汰策略：8 种（Redis 8.6 起 10 种）

源码：`config.c:36-43`（枚举）+ `server.h:556-569`（位标志定义）

| 策略 | 淘汰范围 | 算法 |
|---|---|---|
| `noeviction` | **不淘汰** | 写操作直接返回 OOM error（读和 DEL 仍可用） |
| `allkeys-lru` | 全部 key | 近似 LRU |
| `allkeys-lfu` | 全部 key | 近似 LFU |
| `allkeys-random` | 全部 key | 随机 |
| `volatile-lru` | **仅带 TTL 的 key** | 近似 LRU |
| `volatile-lfu` | **仅带 TTL 的 key** | 近似 LFU |
| `volatile-random` | **仅带 TTL 的 key** | 随机 |
| `volatile-ttl` | **仅带 TTL 的 key** | 剩余 TTL 最小者优先 |

关键配置（`config.c` 源码核对的**默认值**）：

```c
// config.c:3163
createIntConfig("maxmemory-samples", NULL, MODIFIABLE_CONFIG, 1, 64,
                server.maxmemory_samples, 5, INTEGER_CONFIG, NULL, NULL);
// config.c:3177
createIntConfig("active-expire-effort", NULL, MODIFIABLE_CONFIG, 1, 10,
                server.active_expire_effort, 1, INTEGER_CONFIG, NULL, NULL);
// config.c:3060-3062  lazyfree-* 默认全是 0（关闭）
createBoolConfig("lazyfree-lazy-eviction",   ..., server.lazyfree_lazy_eviction,   0, ...);
createBoolConfig("lazyfree-lazy-expire",     ..., server.lazyfree_lazy_expire,     0, ...);
createBoolConfig("lazyfree-lazy-server-del", ..., server.lazyfree_lazy_server_del, 0, ...);
```

- **`maxmemory-samples` 默认 5、范围 1-64** —— 证明 Redis 的 LRU/LFU 是**采样近似**，不是精确 LRU
- **`noeviction` 是默认值**（`config.c:3154`）

> 🆕 **加分点：Redis 8.6.0 新增了 LRM（Least Recently Modified，只有写操作更新访问时间），策略从 8 种变成 10 种** —— `allkeys-lrm` / `volatile-lrm`。`redis.conf` 里已经能查到（本地那份 unstable 的 `redis.conf` 有，7.4.0 的没有）。答"8 种"之后补一句，能证明你在跟进版本。

### 过期删除：两套机制 + 一个必须纠正的八股

**① 惰性删除** —— `db.c:1974 expireIfNeeded()` / `db.c:1928 keyIsExpired()`

每次访问 key 时检查。**最值得说的细节是从库行为**：

```c
// db.c:1991-1994
if (server.masterhost != NULL) {
    if (server.current_client && (server.current_client->flags & CLIENT_MASTER)) return KEY_VALID;
    if (!(flags & EXPIRE_FORCE_DELETE_EXPIRED)) return KEY_EXPIRED;
}
```

→ **从库不自己删过期 key**，只返回 `KEY_EXPIRED`，**等主库下发合成的 DEL**。（源码注释：*"the replica key expiration is controlled by the master that will send us synthesized DEL operations"*）

**② 定期删除** —— `expire.c:187 activeExpireCycle()`

源码核对（**redis-7.4.0**）：

```c
// expire.c:92-96
#define ACTIVE_EXPIRE_CYCLE_KEYS_PER_LOOP 20   /* Keys for each DB loop. */
#define ACTIVE_EXPIRE_CYCLE_FAST_DURATION 1000 /* Microseconds. */
#define ACTIVE_EXPIRE_CYCLE_SLOW_TIME_PERC 25  /* Max % of CPU to use. */
#define ACTIVE_EXPIRE_CYCLE_ACCEPTABLE_STALE 10 /* % of stale keys after which we do extra efforts. */

// expire.c:247  时间预算
timelimit = config_cycle_slow_time_perc*1000000/server.hz/100;

// expire.c:348  ★ 决定要不要再扫一轮
repeat = db_done ? 0 : (data.sampled == 0 ||
         (data.expired * 100 / data.sampled) > config_cycle_acceptable_stale);

// server.h:100
#define CONFIG_DEFAULT_HZ 10
```

### ⚠️ 纠正一个流传很广的八股错误

中文面经普遍写：

> ❌ "每 100ms 随机抽取 20 个 key，**如果超过 25% 过期就继续扫**"

**把 `ACTIVE_EXPIRE_CYCLE_SLOW_TIME_PERC 25`（CPU 时间上限）和真正的重复阈值搞混了。**

从源码看，这是**两个不同的数**：

| 常量 | 值 | 作用 | 源码 |
|---|---|---|---|
| `ACTIVE_EXPIRE_CYCLE_SLOW_TIME_PERC` | **25** | **CPU 时间预算**：每轮 slow cycle 最多占 25% 的 CPU。hz=10 时换算成 `25×1000000/10/100 = 25000µs = 25ms` | `expire.c:94` / `:247` |
| `ACTIVE_EXPIRE_CYCLE_ACCEPTABLE_STALE` | **10** | **重复采样阈值**：本轮采样的 key 里过期比例 **> 10%** 才继续扫下一轮 | `expire.c:95` / `:348` |

而 `config_cycle_acceptable_stale = ACTIVE_EXPIRE_CYCLE_ACCEPTABLE_STALE - effort`（`expire.c:199-200`），默认 `active-expire-effort = 1` → `effort = 0` → **阈值就是 10%**。

> **`>25%（20 个里 >5）` 是 Redis 5.0 及更早的旧逻辑**（`while (expired > ACTIVE_EXPIRE_CYCLE_LOOKUPS_PER_LOOP/4)`）。**6.0 起改成了上面的百分比判据。**
>
> 说"8 种策略、10% 阈值、25% 是 CPU 预算"——这个精度差就是"读过源码"和"背过面经"的分界线。

**其余机制细节**：

- **触发**：slow cycle 经 `serverCron → databasesCron`；hz 默认 10，但 **`dynamic-hz` 默认开启**，客户端多时 `server.hz` 会翻倍直至 `CONFIG_MAX_HZ = 500`
- **另有 fast cycle**：在 `beforeSleep` 每事件循环按需执行，上限 `ACTIVE_EXPIRE_CYCLE_FAST_DURATION = 1000µs`；准入条件是 `stat_expired_stale_perc >= config_cycle_acceptable_stale`（`expire.c:223-225`）
- **7.x 的"采样"已非纯随机**：用 `db->expires_cursor + kvstoreScan` 逐桶扫描，`max_buckets = num*20`（`expire.c:326`）
- **`active-expire-effort` 范围 1-10、默认 1**，会同时放大采样数、fast 时长、CPU 预算，并**降低** stale 阈值
- **异步删除**：Redis 4.0 引入 `UNLINK` / `FLUSHDB ASYNC`，配置键是 `lazyfree-lazy-eviction` / `lazyfree-lazy-expire` / `lazyfree-lazy-server-del`（另有 `lazyfree-lazy-user-del` / `lazyfree-lazy-user-flush`），**默认全关**

### 结合我自己项目的正确答法

> 淘汰 8 种：`noeviction` + `allkeys`/`volatile` × `lru`/`lfu`/`random`/`ttl`（8.6 又加了 `lrm` 两种）。
>
> 删除 = 惰性 + 定期：惰性删除在每次访问时判；定期删除随 serverCron 以 hz=10 触发，每轮从 expires 字典采样 20 个 key，**过期比例超过 10%（不是 25%，25% 是 CPU 时间预算）** 就继续扫，慢周期时间上限是 hz 周期的 25%。
>
> **我们车辆 key 没有 TTL，所以配 `volatile-*` 等于不淘汰、写满 OOM**；而 Redis 又是唯一权威源、没有下游数据库可以回源 —— 所以只能 `noeviction` + **容量告警扩容**，真正扛 QPS 的是本地 LRU 那一层。
>
> 这里还有个连带风险：**`noeviction` 下写满会直接报 OOM error**，读和 DEL 还能用 —— 也就是**权威源写不进去了**，这是必须配内存告警的原因。

### 追问预案

| 追问 | 怎么答 |
|---|---|
| key 都没 TTL，maxmemory 打满会发生什么，写请求报什么错 | 返回 OOM error（`command not allowed when used memory > 'maxmemory'`）。**因为 Redis 是权威源，这是可用性事故不是降级**——必须容量告警 + 扩容 |
| 定期删除会不会阻塞主线程 | 单次有 `timelimit` 硬上限（hz=10 时 25ms），且分 DB 轮流扫描（`current_db` 游标）、单 DB 内有 `max_buckets` 上限，所以是**切片式**的；但 25ms 仍在主线程上，**大量 key 同时过期会造成延迟尖刺**（这也是要打散 TTL 的原因） |
| 硬要上 `volatile-lru`，怎么给车辆 key 加 TTL 又不丢权威数据 | 不能加——TTL 到期 Redis 会删 key，而它是唯一权威源，删了就没了。**除非同时引入真正的持久层**，那是架构改造不是配置调整 |
| `UNLINK` 和 `DEL` 什么区别 | `DEL` 同步释放内存（大 key 会阻塞主线程）；`UNLINK` 只把 key 从 keyspace 摘除，**内存交给后台线程异步释放**。4.0 引入 |

---

## 6. Q6：为什么攒批后落库，有注意你们管道的时延吗？

### 面试官在做什么

问"时延"，其实是在拆"攒批"这个前提：**你为吞吐引入了 60s 的最坏等待，却拿 kafka lag 当性能指标**。

他要的是"**你能不能量化自己引入的代价**"。

### 我当时的答法

复述了攒批机制本身（上游发 msg 频率很快 → 攒批数组 → 满了落库；低峰期没满也有 60s 倒计时兜底），性能观测只说打了 **kafka lag，低峰 100 左右、高峰 1000 左右**。**全程没给任何端到端时延数字**。

### 缺口

**① 把 lag 当成了时延来答**

`lag` 是**积压的消息条数，不是秒**。面试官问时间，你答条数，等于没答。

**换算要分清两个口径**：

```
lag / 消费速率  = 追平积压所需时间 (catch-up time)
端到端消息时延 ≈ lag / 生产速率
```

两者**仅在稳态（生产速率 ≈ 消费速率）时相等**。

**② 没把时延拆成三段**，所以说不清 p50 和 p100 差多少：

| 段 | 量级 | 说明 |
|---|---|---|
| **攒满等待** | 高峰 `batch/速率` = `1000/5000` = **200ms** | ⚠️ 前提是**单个消费者聚合了全部 5k/s**；topic 多分区、每消费者独立攒批时，每批填充速率是 `rate/N`（10 分区/10 消费者 → 2s 才满） |
| **60s 兜底** | 低峰最坏 **60s** | ⚠️ 前提是 60s 为**自由运行的周期 ticker**（则 p100=60s、均值≈30s）。若是"每来一条消息就重置"的 debounce，稳定以 <60s 间隔到达的流量**永远不触发 flush，时延无上界** |
| **落库本身** | ms 级 | batch insert 一次事务 |

**③ 没提攒批数组在进程内存里的丢数据风险**

**④ offset 什么时候提交**——攒批 + 异步提交的组合最容易丢数据

### 正确的答法

> 攒批是**拿时延换吞吐**：batch insert 把 N 次事务提交/fsync 合成 1 次，同时省掉 N-1 次网络 round trip 和语句解析——`innodb_flush_log_at_trx_commit=1` 时约 **10x** 量级（100 行批；5 行批只有约 3x；要到 50-100x 得叠加顺序主键、buffer pool、`sync_binlog` 等整体调优；`trx_commit=2/0` 时没有每 commit 的 fsync 可摊薄，增益掉到约 3-5x）。
>
> 时延分三段：**攒满等待 + 60s 兜底 + 落库本身**。高峰 5k/s、batch=1000 → 攒满约 200ms；低峰最坏 60s。
>
> 我们只打了 kafka lag（100/1000），**但 lag 是条数不是时间**，要除以速率才能还原成时延，而且要看是除以生产速率还是消费速率。
>
> **最大的坑是攒批数组在进程内存里**：K8s 滚动发布 / 进程崩溃时未满批次直接丢，所以 **offset 必须等落库成功再提交**。

### ⚠️ 一个容易讲反的机制：SIGTERM 与 SIGKILL

> ❌ 错误说法："没注册 SIGTERM handler，未满批次会被 SIGKILL 丢弃"

**因果链是反的。** `SIGKILL` **只在**"进程处理了/忽略了 SIGTERM，但过了 `terminationGracePeriodSeconds` 仍没退出"时才发出。

**如果不注册 SIGTERM handler，进程收到 SIGTERM 会立即按默认行为退出**（Go 官方文档：*"A SIGHUP, SIGINT, or SIGTERM signal causes the program to exit"*），内存里的未满批次**当场丢失**，根本没有"等 30s 再被 SIGKILL"这一步。

**正确说法**：

> K8s 默认 `terminationGracePeriodSeconds=30s`，**小于 60s 攒批窗口** —— 等不到自然兜底。
> 正确做法是在 **SIGTERM 钩子里同步 flush 未满批次**，并保证在 grace 内完成（必要时调大 grace）。不要指望 60s 定时器。

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 60s 兜底这条消息，看板要求秒级可见怎么改 | 缩短兜底窗口（如 1s）+ 减小 batch；或对"需要秒级"的指标走独立快通道，不和批量链路混 |
| 进程重启 / 滚动发布时，攒批数组里没落库的数据怎么办，offset 何时提交 | SIGTERM 钩子同步 flush；**offset 必须落库成功之后再提交**（至少一次语义），消费端做幂等 |
| 高峰和低峰用同一个 batch size 吗 | **不该**。固定 batch 在低峰会一直在等攒满 → 只能靠定时器兜底、时延拉满；应做**动态 batch**（按速率调整）或给低峰更小的 batch |
| lag 锯齿为什么是这个形状 | 攒批会让 lag 呈锯齿状，**峰值量级≈batch size**（前提是"每批写完提交一次 offset"；按 poll 提交或开 autocommit 时锯齿更大） |

---

## 7. Q7：售后工单系统：你们怎么做的 mysql 主从同步？

### 面试官在做什么

**不是考"binlog 是什么"，是借主从探你有没有意识到自己那个单点 MySQL 的可用性欠债。**

跟 Q2/Q3 那种"拆优化前提"不同，这题是"**探架构边界**"：你答得越像背概念，越暴露生产里根本没配过。

真考点 = **链路完整度 + 格式选型取舍 + 延迟判断能力**三件套。

### 我当时的答法

> 从节点订阅主节点的 binlog，再重放这些变更。

**但我实际生产上是单点 MySQL，没配主从** —— 所以只在原理层面答了一句，链路没展开，也没说清格式选型、要不要半同步、延迟怎么判。

### 缺口（三层，一层比一层致命）

**① 链路只答了 1/4** —— 主库 dump 线程、从库 IO 线程写 relay log、SQL 线程回放这三段全没说，面试官一听就知道没搭过

**② 缺选型** —— `binlog_format` 没提。`STATEMENT` 遇上 `NOW()`/`UUID()`/`RAND()` 会**主从不一致**，`ROW` 才是一致性默认

**③ 最大的是没主动认账** —— 单点 MySQL 意味着进程/机器挂了**工单系统全停、RPO 等于全量数据**。面试官问"主从同步"八成就是在探这个，我却去讲原理，**等于把最大的洞盖住了**

### 正确的答法

> **工单库当时是单点，挂了就全停、RPO = 全量，这是最大的欠债，我先说这个。**
>
> 要补的话：`binlog_format` 用 **ROW** + **GTID**；链路是
> **主库 dump 线程推 binlog → 从库 IO 线程写 relay log → SQL 线程回放**；
> 开**半同步 `AFTER_SYNC`**（`wait_for_slave_count=1`、`timeout=1000ms`）保证提交至少落到 1 个从库；
> 延迟用 **`pt-heartbeat`** 测，**别信 `Seconds_Behind_Master`**。

### 主从同步链路（完整版）

```
主库
 ├─ 客户端提交事务 → 写 binlog（Server 层）→ 提交
 └─ dump 线程：读取 binlog，推给从库
                          │
从库                      ▼
 ├─ IO 线程：接收 → 写入 relay log（中继日志），并记录已读到的主库 binlog 位点
 │            （在 master.info / mysql.slave_master_info 里）
 └─ SQL 线程：读 relay log → 在从库回放 → 记录已回放位点
```

**三个必须能说的取舍**：

| 点 | 说明 |
|---|---|
| **`binlog_format`** | `ROW`（5.7.7 起默认，8.0 沿用）记录行变更，**确定性**；`STATEMENT` 记录 SQL 原文，遇 `NOW()`/`UUID()`/`RAND()`/`LIMIT` 无主键等**主从不一致**；`MIXED` 由 MySQL 自行切换 |
| **GTID** | 每个事务一个全局唯一 ID，**故障切换后不用手动找位点**；但要注意 GTID 空洞和 `gtid_purged` |
| **半同步** | `AFTER_SYNC`（5.7.2 起默认）在**提交前**等从库 ACK，保证提交至少落 1 从库；`AFTER_COMMIT` 是旧行为，主库崩溃可能丢。超时（`rpl_semi_sync_master_timeout` 默认 10000ms）后**自动降级为异步** |

**从库并行回放**：MySQL 8.0.27 起 `binlog_transaction_dependency_tracking` 默认由 `COMMIT_ORDER` 改为 **`WRITESET`**，显著提升并行度（不再受"同事务组必须串行"的限制）。

### `Seconds_Behind_Master` 为什么会骗你

它的语义是「**从库当前回放到的那个事件的时间戳**」与「**当前时间**」之差 —— **不是端到端复制延迟**。

| 场景 | 表现 | 为什么骗人 |
|---|---|---|
| 从库 SQL 线程停了 | **NULL** | 不是"无延迟"，是"不知道" |
| 主库持续写入、从库暂时追平 | **0** | 只是"已回放的时间戳追上了当前时间"，积压可能还在 |
| 主库长时间无写入 | **0** | 没有新事件可回放而已 |

→ 所以生产要用 **`pt-heartbeat`**（主库定时写心跳表，从库读出来算真实差值）。

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 从库延迟 10 分钟，用户提交完工单立刻刷新看不到，业务怎么解 | 三选一：**GTID 等待**（`WAIT_FOR_EXECUTED_GTID_SET`）/ **写后一段时间强制读主库** / **本地先落一份**。按业务容忍度选 |
| 半同步超时降级成异步，那段时间写入的数据 RPO 就不是 0 了，怎么兜 | 监控降级事件并告警；关键业务可**降级为写失败**而不是静默异步；或提高 `wait_for_slave_count` |
| `binlog_format=ROW` 下 `UPDATE ... LIMIT` 无主键的表，为什么还可能不一致 | ROW 格式记录的是"被影响的行"，但**主从如果选的索引不同、扫描顺序不同**，`LIMIT` 可能命中不同的行 → 仍不一致。所以 ROW 也要**主键 + 唯一索引** |

---

## 8. Q8：数采链路看板 agent：你们前端存的对话，怎么压缩？

### 面试官在做什么

**他问的不是"用什么压缩算法"，是"全量历史为什么每轮都要进请求"。**

我答的"前端存原件 + 后端压缩"，把 **token 成本和网络成本原封不动留在了原地**——压缩只发生在**全量上传之后**。面试官在拆这个前提。

### 我当时的答法（三点论证）

1. 前端 `AiChatPanel.vue` 的 `this.messages` 存**全量**（用户原话、助手文本、`tool_call`、`tool_result` 完整 JSON —— 图表渲染要原始数据），每轮全量序列化 POST 给后端
2. 后端 `buildAnthropicMessages()`：**≤40 条**直接重建协议消息，**>40 条**走 `compactHistory`（滑动窗口 + 摘要）
3. 主动交代了两个已知成本：**请求体积线性增长**、**刷新即丢**
4. 前端存原件是**我自己的架构选择，不是遗漏**

### 缺口（三个口子）

**① 层放错了** —— 压缩在后端，但全量数据是**前端每轮重传**的。模型侧 token 省了，**网络侧一点没省**，n 轮总上传量是 **O(n²)**。

**② 阈值拍脑袋** —— 按"**条数**"切而不是按 **token** 切。一条图表 `tool_result` 的原始 JSON 和一条"好的"文本**差两个数量级**，所以 40 条既可能只有 2k token，也可能 6 万。

**③ 摘要说得太干净** —— 没讲：

- `compactHistory` 到底**丢了什么**
- 重复摘要的**抖动**（同一批历史每轮现算，结果不稳定、不可缓存）
- **`tool_use` / `tool_result` 成对约束** —— 窗口切在中间，Anthropic 接口会**直接报 400**

### 正确的答法

> 一条图表 `tool_result` 约 **4k token**，40 条里 8 条工具结果就 **3 万 token 打底**，而且**还是每轮全量重传** —— n 轮累计上传 `∑i ≈ n²/2`，是 O(n²)。
>
> **改法：历史外置。** 模型侧历史放后端 **Redis（key = sessionId）**，每轮只发**增量**，请求体积从 O(n) 降到 O(1)。这一改同时解决"刷新即丢"。
>
> 其次：**阈值从"40 条"改成"token 预算"**；**摘要首次算完就落库**，不每轮现算。
>
> 最后：滑窗必须保证 **`tool_use` / `tool_result` 成对**，否则 Anthropic 接口 400。

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 摘要之后原始数据丢了，用户回头问"刚才那张图再换成柱状图"怎么办 | 这正是**父子文档**思路：摘要是给模型的，**原始数据留在前端/后端存储里**，需要时按需回填。tool_result 的原始 JSON 不该进摘要 |
| 40 这个数字怎么定的，压到 20 条会掉什么 | 老实说**没有数据支撑，是拍的**。正确做法是换成 token 预算（比如 30k），并按"摘要后正确率掉了多少"来校准 |
| `tool_result` 那坨 JSON 能截断吗，截了模型还答得对吗 | **不能随便截**。图表数据截断后模型对数据的判断会错。要么保留完整、要么把结构化数据转成"摘要 + 统计量"（列名、行数、关键极值）喂进去 |
| 前端为什么不压缩 | 前端保留了原件（`tool_result` 完整 JSON），**主要问题是每轮全量上传而不是格式**；真要压，先做历史外置（省的比压缩大一个量级） |

---

## 9. Q9：lru 手撕

### 面试官在做什么

**不是考你会不会写链表，是考你第 1 题亲口说"用了 golang-lru 的 expirable"，现在验货：你是读过源码还是只会调 API。**

45 分钟一面压轴手撕，面试官看的是**每个设计动作背后的取舍能不能脱口而出**（O(1) 从哪来、dummy 节点干嘛的、锁怎么加），而不是代码能不能跑。

### 我当时的处境

这题其实有优势 —— `expirable` 内部就是 map + 双向链表。但**"用了 expirable"会变成负资产**：面试官在第 1 题已经在问"有调研其他的吗"，就是在验源码阅读。只写出能跑的版本、却答不出 Q1 列的三个坑，前面说得越具体，这里越显得只会调包。

### 实现要点

```go
type node struct {
    key, val   string
    prev, next *node
}

type LRU struct {
    mu       sync.Mutex
    cap      int
    items    map[string]*node
    head, tail *node   // dummy 哨兵
}

func New(cap int) *LRU {
    h, t := &node{}, &node{}
    h.next, t.prev = t, h
    return &LRU{cap: cap, items: make(map[string]*node), head: h, tail: t}
}

func (c *LRU) Get(key string) (string, bool) {
    c.mu.Lock(); defer c.mu.Unlock()
    n, ok := c.items[key]
    if !ok { return "", false }
    c.moveToFront(n)
    return n.val, true
}

func (c *LRU) Put(key, val string) {
    c.mu.Lock(); defer c.mu.Unlock()
    if n, ok := c.items[key]; ok {
        n.val = val
        c.moveToFront(n)
        return
    }
    n := &node{key: key, val: val}
    c.items[key] = n
    c.pushFront(n)
    if len(c.items) > c.cap {
        old := c.tail.prev          // 最久未用
        c.unlink(old)
        delete(c.items, old.key)
    }
}

func (c *LRU) unlink(n *node) { n.prev.next, n.next.prev = n.next, n.prev }

func (c *LRU) pushFront(n *node) {
    n.prev, n.next = c.head, c.head.next
    c.head.next.prev = n
    c.head.next = n
}

func (c *LRU) moveToFront(n *node) { c.unlink(n); c.pushFront(n) }
```

### 四个必须主动讲的取舍（不等面试官问）

| 设计动作 | 为什么 |
|---|---|
| **map + 双向链表** | map 给 O(1) 定位，链表给 O(1) 摘除/插入。**用切片不行——删中间元素要搬移后续元素，是 O(n)** |
| **dummy head / tail** | 省掉"链表为空 / 只有一个节点 / 删的是头或尾"的**所有判空分支**，`unlink`/`pushFront` 不用写 if |
| **`map` 存 `*node` 而不是 index** | 存 index 在链表移动后会失效，还得反查 |
| **`Get` 也要写锁** | 《Get 是写操作》—— 它要 `moveToFront` 改 recency。用 `RWMutex` 而 Get 走读锁是**错的** |

**加锁粒度**：整表一把锁在热点 key 上会串行。**按 key 哈希分 16 段、每段一把锁**，竞争降到约 1/16。（`golang-lru` 的 `lru.Cache` 用 `RWMutex` 但 `Get` 走写锁，本质是同一把锁。）

### 追问预案

| 追问 | 怎么答 |
|---|---|
| 为什么不用 `container/list` | 它存的是 `any`，每次操作要**接口装箱和类型断言**，热点路径多一次分配。自己写 `node` 结构体省掉这层。（顺带：**v2 的 golang-lru 正是为此弃用了 `container/list`，改用自研 `internal.LruList`**；`container/list` 是 v1 的写法） |
| `sync.Map` 能不能替掉 map + 锁 | **不能**。`sync.Map` 只在读多写极少、key 集合稳定时有优势；LRU 每次 `Get` 都含 `MoveToFront` **本质是写操作**，会让 read-only map 反复失效退化回 mutex，**比分段锁更慢** |
| 要给这个 LRU 加 TTL 怎么做 | 参考 expirable 的两层：**惰性删除**（`Get`/`Peek` 时比 `expiresAt`，**精确**）+ **后台 ticker 兜底**（周期 `ttl/100`，只负责释放内存）。⚠️ 注意**生效 TTL 恰好等于 ttl**，不是 `[ttl, ttl+ttl/100]`（见 Q1） |
| 命中率还能更高吗 | 换 **ristretto** 的 **TinyLFU 准入 + cost-based 淘汰**：能挡住"扫一遍全表"这种污染 LRU 的访问模式；代价是要自己接 TTL 和指标 |
| 怎么做到并发安全又不串行 | 分段锁；或**读路径无锁 + 单写者**（channel 串行化写操作）。前者简单，后者吞吐更高但复杂 |

---

## 10. 复盘：这场面试暴露的三件事

### ① 我的优化没有被度量过

这是**贯穿全场的主线**。Q1 说选型 → Q2 拆 QPS → Q3 要 p95 → Q4 问代价，是一个完整的"**你说你优化了，证据呢**"的递进。

**补法**（很具体，成本也低）：

- 给 Redis 调用埋**独立的 P50/P95/P99** 埋点（不只看接口整体 P99）
- 记录**本地缓存命中率**
- 做一次**开关对比**：本地缓存开 / 关，看接口 P99 的变化

> 有了这三个数，Q1-Q4 是**一道题**而不是四道。

### ② 架构自述里的每一句都会被反推

我说"Redis 没有 TTL" → Q5 立刻问淘汰策略，因为**没有 TTL 意味着 `volatile-*` 全是死策略**（这是我当时完全没意识到的洞）；Redis 是唯一权威源 → Q4 的一致性风险没有兜底。

**教训**：说出口的每个架构事实，都要在脑子里过一遍"**这句话推出什么反面**"。

### ③ "用现成的库"要配得上"读过源码"

我在第 1 题主动说了用 `expirable`，第 9 题就手撕 LRU —— 这是面试官的常规连招。

**如果不打算读源码，就不要在简历/自述里把库名说得那么具体**；一旦说了，`Add` 续期 TTL、`size=0` 关掉 LRU、`ttl<=0` 变 10 年这些坑就得能答。

### 附：这场面试的"如果重来"版本（Q1 开场怎么答）

> 我们在车辆数据平台上加了一层进程内 LRU，用的是 hashicorp/golang-lru v2 的 **expirable** 子包。
>
> **选型维度是四个**：容量上限、TTL、泛型、源码可读性。expirable 一包给全 LRU + TTL + 后台清理，零依赖、150 行能读完；如果命中率是首要指标会换 ristretto，go-cache 没有容量上限所以排除。
>
> **收益我先说清边界**：高峰期 Redis 只有 5-6k QPS，对单实例（10 万 QPS 量级）只占 5%，**拿吞吐当理由站不住**。本地缓存真正省的是**尾延迟**——本地命中百纳秒级，同机房 Redis GET 中位数 0.2-0.5ms，差三个数量级；而且 Redis 的低平均负载不代表低 P99（fork、大 key 过期都会拉尖）。
>
> **代价是最终一致**：变的是 value 不是 key，VIN 不变但项目归属和车型会改。Redis 是唯一权威源又没有 TTL，所以靠本地 TTL 封顶脏读窗口，写路径要广播失效。这里我知道 `expirable` 有个坑——`Add` 已存在的 key 会续期 TTL，所以**不能实现成"读命中后回写"**。
